import 'package:flutter/painting.dart';

abstract class AppRadius {
  AppRadius._();

  /// 0px
  static BorderRadius radiusZero = BorderRadius.zero;

  /// 4px
  static BorderRadius radius2 = BorderRadius.circular(2);

  /// 4px
  static BorderRadius radius2xs = BorderRadius.circular(4);

  /// 6px
  static BorderRadius radius3xs = BorderRadius.circular(6);

  /// 8px
  static BorderRadius radiusXs = BorderRadius.circular(8);

  /// 12px
  static BorderRadius radiusSm = BorderRadius.circular(12);

  /// 16px
  static BorderRadius radiusMd = BorderRadius.circular(16);

  /// 20px
  static BorderRadius radiusLg = BorderRadius.circular(20);

  /// 24px
  static BorderRadius radiusXl = BorderRadius.circular(24);

  /// 28px
  static BorderRadius radius2xl = BorderRadius.circular(28);

  /// 32px
  static BorderRadius radius3xl = BorderRadius.circular(32);

  /// 36px
  static BorderRadius radius4xl = BorderRadius.circular(36);

  /// 40px
  static BorderRadius radius5xl = BorderRadius.circular(40);
}
