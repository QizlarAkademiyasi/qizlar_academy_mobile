/// Kunlik tanga / streak (prefetch, bottom sheet, claim).
///
/// `false` bo‘lsa — GET/claim chaqiruvlari, avto-sheet va Tangalar kartasidan ochilish ishlamaydi.
const bool kDailyCoinFeatureEnabled = true;
