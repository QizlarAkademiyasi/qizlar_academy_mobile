enum StorageKey {
  accessToken,
  refreshToken,
  userType,
  tokenType,
  language,
  fcmToken,
  pinCodeExist,
  theme,

  userDashboard,
  userMe,
  userId,

  authKey,
  authKeyInfo,

  partIds,
  videoProgress,
}