/// Firebase Remote Config — funksional kalitlar (konsol bilan mos).
abstract final class RemoteConfigFeatureKeys {
  RemoteConfigFeatureKeys._();

  /// `true` bo‘lsa kirish ekranida Google Sign-In tugmasi ko‘rinadi.
  static const String googleSignIn = 'google_sign_in';
}
